# Vigilia — идея, архитектура, план

## 1. Идея

**Vigilia** (лат. «бодрствование») — маленькое приложение для Windows, которое не даёт ПК уйти в сон.

Главное — одна большая кнопка-«глаз» (орб):
- **выкл** — глаз закрыт, орб тёмный, «спит»;
- **вкл** — глаз открыт и изредка моргает, орб залит акцентом, вокруг «дышит» аура и вращается пунктирная орбита.

Чтобы приложением было удобно пользоваться, одной кнопки мало. Добавлено то, без чего такие утилиты неудобны:
1. **Экран** — «Гаснет / Не гаснет»: держать ли включённым ещё и дисплей (для презентаций и видео) или только систему (для загрузок и рендера).
2. **Таймер** — ∞ / 30м / 1ч / 2ч / 4ч. Кольцо прогресса вокруг орба и обратный отсчёт; по истечении режим выключается сам.
3. **Трей** — закрытие окна прячет его в трей, режим продолжает работать. Иконка в трее меняется по состоянию; левый клик показывает окно, в меню есть «Вкл/Выкл», «Показать» и «Выход».
4. **Автозапуск** с Windows (стартует свёрнутым в трей) и **звуки** — оба переключаются в настройках.
7. Настройки по умолчанию скрыты и открываются кнопкой: панель «вырастает» из неё (добавлено во второй итерации, T10).
5. Приложение запоминает выбранные настройки и включённое состояние без таймера.
6. Запускается только одна копия: повторный запуск показывает уже открытое окно.

Визуальный язык: тёмная палитра Catppuccin Mocha с фиолетовым подтоном во всех слоях, моноширинный шрифт, один акцент, «тройной отклик» на клик (поп, белая вспышка, звук), переключатель-«гусеница», toggle с OutBack, посимвольное появление текста и «дыхание» ауры.

## 2. Архитектура

**Стек:** Flutter (Windows desktop). Он уже настроен на этой машине и знаком по Enitor/Ledgify, а анимации на 60 fps — его сильная сторона. Системные вызовы Windows делаются через `dart:ffi` без обёрток.

**Зависимости:** `window_manager` (окно без рамки, скрытие вместо закрытия), `tray_manager` (иконка в трее), `ffi` (строки UTF-16 и выделение памяти для WinAPI).

```
lib/
  main.dart              бутстрап: окно, трей, контроллер, runApp
  theme.dart             токены: цвета, длительности, кривые (OutBack(s)), lighter/darker
  win32.dart             FFI: PowerCreateRequest/PowerSetRequest/PowerClearRequest,
                         PlaySound, SystemParametersInfo (reduce motion)
  sounds.dart            синтез коротких WAV в памяти (свои звуки, без чужих файлов)
  settings.dart          JSON в %APPDATA%\Vigilia\settings.json; автозапуск через reg.exe (HKCU\...\Run)
  controller.dart        VigilController (ChangeNotifier): active, keepDisplay, timer, endsAt,
                         sounds, autostart; тик таймера; применение power request
  shell.dart             трей + окно: иконка/подсказка/меню по состоянию, скрытие в трей
  ui/
    home.dart            экран: заголовок, орб, статус, карточки настроек
    title_bar.dart       свой заголовок окна (перетаскивание, «в трей»)
    pressable.dart       тройной отклик: hover/press-масштаб, поп, вспышка
    orb.dart             орб: глаз (CustomPainter), аура, орбита, кольцо таймера
    segmented.dart       сегментный переключатель «гусеница»
    toggle.dart          toggle (ручка OutBack 1.2)
    settings_drawer.dart панель настроек, вырастающая из кнопки (T10)
    motion.dart          текст с посимвольным появлением (Letters) и стартовый каскад (Cascade)
windows/runner/main.cpp  одна копия (mutex) + показ существующего окна
assets/tray_on.ico, tray_off.ico, windows/runner/resources/app_icon.ico  (tool/make_icons.py)
test/                    юнит-тесты: цвета, WAV, логика таймера контроллера; smoke-тест виджета
```

**Поток данных:** UI → `VigilController` (единственный источник правды) → `win32` (запрос питания) + `settings` (сохранение) + `sounds`; `shell` и UI слушают контроллер.

**Как не даём уснуть:** `PowerCreateRequest("Vigilia: …")` + `PowerSetRequest(SystemRequired [, DisplayRequired])`. В отличие от `SetThreadExecutionState`, запрос не привязан к потоку и виден в `powercfg /requests` с понятной причиной. Когда процесс завершается, Windows снимает запрос сама, так что «зависнуть» во включённом состоянии нельзя.

## 3. План разработки (задача → жёсткие требования к сдаче)

**T1. Каркас и токены.** Проект Flutter (Windows), зависимости, `theme.dart`.
- ✅ `flutter pub get` без ошибок; `flutter analyze` — 0 issues.
- ✅ `lighter`/`darker` работают как Qt через HSV и покрыты тестом (известные значения).
- ✅ `OutBack(s)` даёт 0 при t=0, 1 при t=1 и перелёт выше 1 при s>0; покрыто тестом.

**T2. Системный слой (FFI).** `win32.dart`: power request, PlaySound, reduce-motion.
- ✅ После включения `CallNtPowerInformation(SystemExecutionState)` показывает флаг `ES_SYSTEM_REQUIRED`, а в режиме «экран не гаснет» ещё и `ES_DISPLAY_REQUIRED`. После выключения флаги снимаются. Проверено на реальной сборке.
- ✅ Повторное включение и выключение не утекает хэндлами: запрос создаётся один раз и закрывается при выходе.
- ✅ Ошибки WinAPI не роняют приложение: возвращается false и статус «ошибка».

**T3. Звуки.** `sounds.dart`: синтез WAV (on / off / tick / done), воспроизведение через PlaySound(SND_MEMORY|SND_ASYNC).
- ✅ WAV-заголовок корректен (RIFF, размеры, 44.1 кГц, 16 бит, моно) — тест.
- ✅ Нет щелчков: огибающая с атакой и затуханием, первый и последний сэмплы ≈ 0 — тест.
- ✅ Буферы живут всё время работы (не освобождаются во время async-проигрывания).

**T4. Контроллер и настройки.** `controller.dart`, `settings.dart`.
- ✅ Логика покрыта тестами: вкл/выкл, смена режима экрана на лету, таймер истекает и выключает, смена таймера во время работы перезапускает отсчёт, ∞ не истекает.
- ✅ Настройки сохраняются и читаются; битый или отсутствующий JSON не роняет приложение (дефолты).
- ✅ Включённое состояние без таймера восстанавливается при старте, с таймером — нет.
- ✅ Автозапуск пишет и удаляет значение в `HKCU\...\Run`, а состояние читается из реестра — проверено вручную через `reg query`.

**T5. UI-компоненты.** pressable, segmented, toggle, motion, orb.
- ✅ Параметры моушена: цвет 180ms, масштаб 250ms OutQuint, поп/вспышка по «весу» действия, «гусеница» 200/350ms OutExpo, ручка toggle 200ms OutBack(1.2), дыхание 1800ms InOutSine.
- ✅ Всё управляется с клавиатуры (Tab/Space/Enter/стрелки), фокус видим (обводка акцентом).
- ✅ Reduce motion: без попов, дыхания, вращения и моргания; цвет, прозрачность и вспышка остаются.
- ✅ Нет утечек: все AnimationController освобождаются в dispose.

**T6. Экран и окно.** home, title_bar, frameless-окно, фиксированный размер.
- ✅ Окно без рамки 380×700 (клиентская часть ≈364×691), не растягивается, перетаскивается за заголовок, по центру экрана.
- ✅ Никаких переполнений (overflow) и обрезанного текста; скриншот сборки проверен глазами.
- ✅ Нет вспышки белого/пустого окна при старте (окно показывается после первой отрисовки).

**T7. Трей и одна копия.** shell.dart, main.cpp, иконки.
- ✅ Крестик прячет окно в трей, а режим продолжает работать.
- ✅ Иконка и подсказка трея меняются по состоянию; меню «Вкл/Выкл · Показать · Выход» работает.
- ✅ «Выход» снимает запрос питания и закрывает процесс.
- ✅ Второй запуск exe не создаёт второй процесс, а показывает первое окно.
- ✅ `--hidden` (автозапуск) стартует сразу в трей.

**T8. Финальная проверка.** `flutter analyze`, `flutter test`, Release-сборка, ручной прогон сценариев, построчная перечитка кода.
- ✅ 0 issues, все тесты зелёные, `flutter build windows --release` успешен.
- ✅ Сценарии T2/T4/T7 повторены на Release-сборке.
- ✅ README с описанием и сборкой.

**T9. Отчёт** (`docs/REPORT.md`) и сверка отчёта с кодом.

**T10. Скрытые настройки (вторая итерация).** Настройки скрыты по умолчанию, открываются кнопкой `settings_drawer.dart`; из проекта убраны упоминания внешнего референса.
- ✅ При старте настройки не видны и недоступны ни мышью, ни клавишей Tab (тест).
- ✅ Кнопка открывает и закрывает панель; Esc сначала закрывает панель, потом прячет окно (тест).
- ✅ Форма «панель + кнопка» — одна фигура с вогнутыми скруглениями на стыке; закрытая — пилюля (тест геометрии).
- ✅ Открытие 450ms OutQuint, закрытие 300ms InCubic; контент проявляется позже формы; строки въезжают каскадом; при reduce motion — без движения.
- ✅ Орб плавно уменьшается, ничего не переполняется; фокус не теряется при закрытии панели.
- ✅ Кадры анимации проверены отрисовкой в тестовом окружении с настоящими шрифтами (без запуска приложения).
- ✅ поиск по названию референса по проекту ничего не находит; analyze 0 issues, все тесты зелёные, Release собирается.

---

# Итерация 3: всё из списка «что бы улучшил / добавил»

## Идея

Сделать Vigilia полноценной утилитой: не только «вкл/выкл», но и «до каких пор» (таймер, пока работает процесс, пока идёт загрузка), расписание, горячая клавиша, уведомления, выбор цвета и английский язык, установщик. Настройки стало много, поэтому в панели появились разделы «Режим / Расписание / Общие» на том же переключателе-«гусенице».

## Архитектура (изменения)

```
windows/runner/native_shell.{h,cpp}  свой трей (иконка, подсказка, меню), уведомления (balloon → toast),
                                     глобальная клавиша Ctrl+Alt+V; канал vigilia/native. tray_manager удалён.
lib/native.dart        Dart-сторона канала
lib/strings.dart       все тексты RU/EN
lib/schedule.dart      окно расписания (чистая логика, тесты)
lib/probes.dart        «процесс запущен?» (Toolhelp32 через FFI) и «сколько байт принято» (netstat -e)
lib/theme.dart         палитра строится из акцента (оттенок акцента подмешан во все слои)
lib/controller.dart    ручной режим + условие окончания + расписание; clock-тик без пересборки всего экрана
lib/ui/pages.dart      страницы панели: Режим, Расписание, Общие, выбор процесса
installer/vigilia.iss  установщик Inno Setup; .github/workflows/build.yml — сборка в CI
```

## План

**T11. Нативная оболочка.** Трей, меню, уведомление, горячая клавиша в C++; `tray_manager` удалён.
- ✅ сборка без `tray_manager`, C++ компилируется; Dart-сторона (подсказка, пункты меню по языку) покрыта тестом
- ✅ иконка, меню, клик, уведомление (`NIF_INFO`), восстановление по TaskbarCreated и удаление иконки при выходе реализованы
- ⚠️ в работающем приложении проверяет пользователь: приложение и клики по рабочему столу я не запускаю

**T12. Уведомления.** Когда условие закончилось (таймер, процесс, загрузка, конец расписания) и окно скрыто — уведомление Windows.
- ✅ тест: контроллер сообщает причину окончания; Shell показывает уведомление только при скрытом окне

**T13. Переключатели — одна остановка Tab.** В обход Tab попадает только выбранный сегмент, стрелки/Home/End двигают выбор.
- ✅ тест: Tab с орба → «Экран» → раздел/таймер, стрелки меняют значение

**T14. Автозапуск следует за exe.** При старте (Release) значение в реестре обновляется, если путь устарел.
- ✅ тест разбора вывода `reg query`; ручная проверка записи/очистки

**T15. Экран не пересобирается каждую секунду.** Тик обновляет только статус и кольцо.
- ✅ тест: тик не вызывает `notifyListeners`, но двигает `clock`

**T16. Глобальная клавиша Ctrl+Alt+V** вкл/выкл, переключатель в «Общих»; если клавиша занята — переключатель гаснет с подсказкой.
- ✅ тест контроллера с фейковой регистрацией; `RegisterHotKey(Ctrl+Alt+V)` проверен на системе прямым вызовом (свободна, повтор отклоняется)
- ⚠️ нажатие клавиши в работающем приложении проверяет пользователь

**T17. «До каких пор»: всегда / таймер / пока работает процесс / пока идёт загрузка.**
- ✅ тесты: процесс завершился → выкл; процесс не запущен → не включаемся, подсказка; загрузка < 100 КБ/с 2 минуты → выкл, всплеск сбрасывает отсчёт
- ✅ выбор процесса из списка запущенных с поиском; разбор `netstat -e` проверен на системе

**T18. Расписание** (дни недели + с/до, шаг 30 минут, окна через полночь).
- ✅ тесты окон (будни, выходные, через полночь); в окне — включено «по расписанию»; выключение вручную пропускает текущее окно

**T19. Цвет акцента (6 вариантов) и язык RU/EN** (по умолчанию — язык системы).
- ✅ палитра строится из акцента, смена — плавный кроссфейд; все тексты (включая трей и уведомления) переведены, тест на отсутствие пустых строк

**T20. Установщик** Inno Setup (ярлык, автозапуск по желанию, чистка реестра при удалении) + сборка в GitHub Actions. Подпись не делаем.
- ✅ `iscc` собирает установщик локально; workflow валиден

**T21. Проверка и отчёт.** analyze 0, все тесты, Release, кадры UI отрисованы (без запуска приложения), построчная перечитка, отчёт обновлён и сверен с кодом.

---

# Итерация 4: доработки после отчёта 1.1

## Идея

Закрыть слабые места из ответов 1.1: потерю фокуса, путаницу процессов с одинаковым именем, грубый замер загрузки, одноцветный трей и фиксированную горячую клавишу. Добавить то, чего не хватало: несколько процессов, подсказку о ближайшем окне расписания, автообновление с кнопкой «удерживайте, чтобы обновить» (раздел 7.8 дизайн-системы) и тесты нативной части. Режим «пока играет видео/звук» не делаем: Windows и плееры уже сами не дают уснуть.

## Архитектура (изменения)

```
lib/hotkey.dart              сочетание (MOD_* + VK_*), таблица клавиш, подписи
lib/updater.dart             GitHub Releases: проверка, скачивание, SHA-256, тихий установщик
lib/ui/hotkey_field.dart     запись сочетания «клавишами»
lib/ui/hold_button.dart      кнопка удержания с волной
lib/win32.dart               + путь exe (QueryFullProcessImageNameW), GetIfTable2, курсор над окном
lib/probes.dart              процессы по пути, адаптеры, скорость по счётчикам адаптеров
lib/ui/pages.dart            + выбор адаптера, мультивыбор процессов, раздел «Система»
windows/runner/test/         тесты native_shell.cpp (CMake, поддельный мессенджер)
assets/tray/                 иконки трея для каждого цвета
installer/vigilia.iss        режим /UPDATE: ждёт закрытия Vigilia и запускает её после установки
```

## План

**T22. Глаз не «сбрасывается» при кликах.** При нажатии кнопки мыши Windows присылает ложный уход курсора из окна.
- ✅ уход курсора учитывается, только если WinAPI подтверждает, что курсор не над окном Vigilia
- ✅ при включении глаз сразу смотрит на курсор, а не ждёт движения мыши
- ⚠️ в работающем приложении проверяет пользователь

**T23. Фокус переживает смену цвета и языка.**
- ✅ тест: после смены цвета мышью пробел включает режим; после смены языка стрелкой фокус остаётся на переключателе языка; без исправления тест падает

**T24. Процессы по пути и несколько сразу.**
- ✅ процесс сравнивается по полному пути exe; путь недоступен — по имени
- ✅ до 10 программ, режим держится, пока работает любая; строки с одинаковыми именами подписаны папкой
- ✅ настройки 1.1 (одно имя) переносятся
- ✅ тесты контроллера, настроек и настоящего списка процессов (путь собственного процесса)

**T25. Загрузка через GetIfTable2, выбор адаптера и порога.**
- ✅ смещения MIB_IF_ROW2 сверены с Get-NetAdapterStatistics; фильтры драйверов, петля и туннели отброшены
- ✅ «все» = физические адаптеры (VPN не считается дважды); подключившийся адаптер не даёт скачка скорости
- ✅ порог 50 КБ/с / 100 КБ/с / 500 КБ/с / 1 МБ/с; адаптер выбирается из списка; тесты

**T26. Иконка трея в цвете акцента.**
- ✅ 12 иконок (вкл/выкл × 6 цветов) из `tool/make_icons.py`, тон тёмных слоёв как у палитры; просмотрены
- ⚠️ в трее проверяет пользователь

**T27. Своя горячая клавиша.**
- ✅ клик по клавишам → запись; нужен Ctrl, Alt или Win (иначе поле трясётся); Esc, клик мимо и уход из окна отменяют
- ✅ на время записи старое сочетание снято; переключатель показывает прежнее состояние
- ✅ тесты контроллера и записи в интерфейсе
- ⚠️ нажатие сочетания в работающем приложении проверяет пользователь

**T28. Тесты нативной части.**
- ✅ `windows/runner/test`: протокол канала, регистрация/смена/снятие сочетания, занятость, маршрутизация сообщений; тест ловит поломку снятия сочетания; запускается в CI
- трей и меню не тестируются: иконка появилась бы у того, кто запускает тест

**T29. Подсказка о ближайшем окне расписания.**
- ✅ «Расписание: завтра в 09:00» под глазом, пока режим выключен, и в разделе «Расписание»; после пропуска окна — следующее
- ✅ тесты `nextStart` и подписи (включая переход на зимнее время)

**T30. Автообновление с кнопкой удержания.**
- ✅ проверка GitHub Releases при старте и раз в 12 часов (только Release-сборка), кнопка «проверить», переключатель автопроверки
- ✅ кнопка по разделу 7.8: заливка 800·(1−уровень) InSine / стекание 1500·уровень OutQuad, волна, буквы на фронте, вспышка 0.5, поп 0.95, звуки «зарядки» и «удара», клавиатура, reduce motion
- ✅ скачивание во временную папку, проверка размера и SHA-256 (если GitHub его отдаёт), тихий установщик `/UPDATE`, выход
- ✅ установщик ждёт закрытия Vigilia в `InitializeSetup` (по исходникам Inno Setup вызывается до проверки AppMutex) и запускает новую версию
- ✅ портативная копия открывает страницу релиза
- ✅ тесты: разбор релиза, версии, хеш, все ветки обновления, кнопка мышью и клавиатурой, UI раздела
- ⚠️ настоящее обновление проверяется после первого релиза с установщиком

**T31. Раздел «Система».** Горячая клавиша, автозапуск и обновления вынесены из «Общих»: иначе панель не помещалась в окно.

**T32. Проверка.** analyze 0, все тесты, тесты C++, Release, установщик 1.2.0, кадры UI отрисованы и просмотрены, построчная перечитка, отчёт обновлён.
